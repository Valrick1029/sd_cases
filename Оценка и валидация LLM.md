Я бы решал эту задачу так:

Использовал метрики:
Классификация -- Recall, F1, ROC-AUC, матрица ошибок для наглядности
Текст -- BLEU, ROUGE, Perplexity

Еще можно посмотреть функционально, то есть как генерирует ответы: токсичность, стабильность, галлюцинации

Постарался правильно разделить данные на train/val/test, то есть учесть дисбаланс классов (если классы есть), время сбора данных, еще чего-нибудь (надо в данные посмотреть)

Также можно сравнивать через семантическую близость эмбеддингов ответов с эталонными (golden set), использовать LLM-Judge, или смешать человеческие ответы с синтетическими.

После деплоя A/B